# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to \`.env\`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# Postgres connection string (local dev: any vanilla Postgres; deployed: RDS via infra/).
POSTGRES_URL="postgres://postgres:postgres@localhost:5432/wholesum"


# You can generate the secret via 'openssl rand -base64 32' on Unix
# @see https://www.better-auth.com/docs/installation
AUTH_SECRET='supersecret'

# Google OAuth provider
# @see https://www.better-auth.com/docs/authentication/google
AUTH_GOOGLE_ID=''
AUTH_GOOGLE_SECRET=''

# Instacart IDP (Developer Platform) API key — point INSTACART_API_BASE_URL
# at the dev sandbox; unset it for production.
INSTACART_API_KEY=''
# INSTACART_API_BASE_URL='https://connect.dev.instacart.tools'

# Set by infra (SST) on the deployed web Lambda; leave unset locally —
# plan creation then logs a visible no-op and plans stay pending.
# PLAN_QUEUE_URL=''